import React from 'react';
import Register from '../auth/Register';
import Table from './Table';

const Home = () => {
    return (
        <div>
            <Table></Table>
        </div>
    );
};

export default Home;